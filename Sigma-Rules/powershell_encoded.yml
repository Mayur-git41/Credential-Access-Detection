title: Suspicious PowerShell Encoded Command
id: 7f86f0a2-1111-4444-8888-123456789abc
status: experimental
description: Detect PowerShell encoded command execution
author: <your_name>
date: <present_date>

logsource:
  product: windows
  category: process_creation

detection:
  selection:
    CommandLine|contains:
      - '-enc'
      - '-encodedcommand'
  condition: selection

level: high
